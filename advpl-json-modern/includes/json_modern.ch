/*
    Definições de sintaxe para facilitar uso de JSON
    Identificação: trsilva23
*/

// Traduz a sintaxe antiga [# 'chave'] para o padrão nativo ['chave']
#xtranslate \[ \# <cKey> \] => \[ <cKey> \]
#xtranslate \[ \# <cKey> \] := <xValue> => \[ <cKey> \] := <xValue>
